# Karg-Thompson-Algorithm---Closed-Jobs-Sequence-Route-Minimizer
This repository contains a personal implementation of the Karg &amp; Thompson algorithm to minimize travel time in a closed sequence of jobs. The method is heuristic, so the result is a local optimal solution and may not represent the global optimum.
